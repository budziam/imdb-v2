version: '3'
services:
  app:
    build: .
    volumes:
      - ./:/app
    depends_on:
      - postgres
    ports:
      - "${APP_PORT}:${APP_PORT}"

  postgres:
    image: postgres:11.1-alpine
    environment:
      POSTGRES_PASSWORD: "${DB_PASSWORD}"
